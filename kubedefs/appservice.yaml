apiVersion: v1
kind: Service
metadata:
  name: vproapp-service
spec:
  selector:
    app: vproapp        # This MUST match the label from your 'kubectl get pods' output
  ports:
  - port: 8080
    targetPort: vproapp-port
    protocol: TCP 
  type: ClusterIP
